<h2><%= "Welcome User : #{@first_name}"%></h2>

<h1>The List!</h1>



<div class="card-body text-center mx-auto" style="width: 12rem; ">
  <% Gossip.all.each_with_index do |gossip, index|  %>
            <div class="card text-center" style="width: 18rem;">
                  <h4 class="card-title"> <%= gossip.title %></h4>
                  <h5 class="card-title">  <%= gossip.user.first_name %></h5>
                  <%= link_to "Rumor n°#{index + 1}", dyn_pages_rumor_path(index + 1), class:"btn btn-primary" %>
            </div>
<br>
      <% end %>
</div>
    
  
